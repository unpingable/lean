# V15 formal dependency integration

Date: 2026-07-22

This record admits the ratified v15 surfaces to canonical, non-default public
build targets. It does not promote a stable surface, change a default target,
or perform a release action.

The dependency order is:

1. the existing public Governed Transport calculus;
2. the existing public Execution Custody calculus;
3. `Continuity.Admission`;
4. `StaticRole`, closed at R3;
5. the PJ core and its calculus-specific adapters;
6. the isolated PJ qualification leaves and final Atlas verdict;
7. external reconciliation with the frozen v15 Inquiry and Preparation
   objects, recorded separately.

`V15Integration` owns the canonical formal roots
`Continuity.Admission`, `StaticRole`, and `PJ`. `V15IntegrationQualification`
owns the seven isolated qualification leaves and the five declaration-dump
roots. Both targets are explicit, Mathlib-free, public-evidence targets.

PJ imports rather than owns or replaces its source calculi. Its exact result is
`ATLAS`: faithful calculus-specific mappings with receipt-index preservation
and exact-receipt anti-minting, not a shared algebra or universal calculus.
The frontier, ownership, context-transport, and residual-theory negative
classifications remain authoritative.
