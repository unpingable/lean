# GT-C03 Public Admission Rationale

**Date:** 2026-07-20

**Verdict:** `C03-PUBLIC-ADMISSION-CANDIDATE`

## Narrow question

This campaign asks only whether the already-qualified private C03
`SpineProjection` crossing is independently entitled to public standing. It
does not reopen GT-4A, redesign the GT core, or enlarge the stable public
contract.

The answer is yes at the candidate gate: C03 establishes one additional exact
external correspondence over unchanged public endpoints, with no declaration,
axiom, dependency, or disposition drift.

## Why the correspondence is substantive

C03 is not a namespace alias or an adapter whose laws reduce to its
constructor fields. It instantiates the public governed-transport vocabulary
against the independently admitted Weathering governed family and exact spine
encoding.

The exact earned surface is bounded to:

- identity lifting of each native source claim into the crossing;
- constructive point coverage of its unchanged funnel target;
- positive transport retaining the exact native witness;
- negative transport retaining the selected native refusal and decision
  equality;
- separate target-local reliance after transport;
- exact refusal recovery through the existing lossless decoder;
- authority non-amplification;
- retention of source authority and source custody consequences;
- a constructive `missingWitness` target gap; and
- the exact distinction between target-local negative evidence and transported
  source evidence.

All 82 compiled target declarations correspond exactly to the frozen C03
source declarations after only the admitted import and namespace mappings.
The correspondence changes no endpoint theorem and introduces no new generic
law.

## Why the dependency boundary is admissible

The target depends exclusively on already-public stable GT, PathVerdict,
Admissibility Calculus, and Weathering modules. Its closure contains no private
adapter, campaign record, scratch namespace, hostile fixture, temporary dump
module, or Mathlib import.

The source-only `LosslessEncodingCollapse` import is adverse evidence, not a
semantic dependency. The compiled source reference census names no declaration
from it, and the module contributes no instance or attribute. The target
therefore removes that import instead of laundering private hostile machinery
into a public instance.

The namespace mapping is owner-specific and injective. In particular,
Weathering declarations map into
`Admissibility.Calculus.Instances.Weathering`; they are not swept through a
broad `CrossCalculus` prefix rewrite. This prevents namespace equality from
standing in for declaration-content preservation.

## Why K05 remains preserved

K05 is not copied into the C03 target leaf. Its exact source countermodel
remains frozen separately at blob
`b448b82cd248ecedb926bd95938ccf958044ac92`. The public endpoint already proves
that exact refusal packets cannot collapse and that the stale and retired
Weathering refusals remain distinct. C03 is parameterized by that repaired
`LosslessEncoding` contract.

Thus the false K05 lift remains defeated without making a private hostile
module a target dependency or claiming that the adverse specimen itself has
received public custody.

## Why K18 remains preserved

K18 is intrinsic to C03 rather than an external dependency. The target leaf
constructs the `missingWitness` local refusal and its exhibited image gap, then
proves that neither imported-positive nor imported-negative evidence can name
that target. The combined theorem retains all three facts together.

Admission therefore cannot reinterpret decoder `none`, target-local refusal,
or lack of coverage as a transported native refusal.

## Why this does not widen the public constitution

The stable GT root, all ten stable GT leaves, the repository root, Lake target
set, target registry, and stable-surface registry remain unchanged. The
existing 704 GT-4A declaration-bearing leaves remain byte-exact.

C03 is classified as one `PUBLIC-EVIDENCE` instance leaf. The evidence
aggregate gains exactly one import so that the independently qualified object
is visible on the evidence surface. No C03 declaration enters the stable GT
root, and the stable core acquires no replay, decoder, Weathering, or
instance-specific field.

The disposition remains exactly:

```text
C03 SpineProjection: SECONDARY-QUALIFIED
```

Public candidate admission does not turn that bounded disposition into global
coverage, composition, repair, or operational correspondence. It has no
effect on C01, C02, or C04 and does not disturb `NO-GENERIC-EXTENSION-EARNED`
or `LAW-FAMILY-ONLY`.

## Hostile questions and results

| Attempted overclaim | Result |
| --- | --- |
| declaration type or body changes under relocation | refused by exact 82-declaration normalized-content comparison |
| axiom footprint changes | refused by exact per-declaration axiom comparison |
| private or campaign dependency enters the target closure | refused; forbidden dependency count is zero |
| broad namespace rewrite launders endpoint identity | refused by owner-specific injective mapping |
| source hostile import becomes a target prerequisite | refused; the unused K05 module is excluded |
| K18 target-local evidence is called transported evidence | refused by the carried `missingWitness` theorem family |
| bounded qualification becomes global coverage or repair | refused by the retained nonclaim surface |
| C03 changes the stable root | refused; stable root and all stable leaves are byte-exact |

The executable admission checker passed all 82 declaration comparisons and
eight representative hostile mutations.

## Decision

C03 earns a public admission **candidate** because it adds a genuine,
externally usable, independently mapped instance correspondence while leaving
the stable constitution and every excluded scientific claim intact.

It does not yet have active public custody. Operator ratification must bind an
exact serialized candidate commit and tree before the target may be described
as admitted.
