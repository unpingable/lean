# PJ Tranche B-prime Operator Ratification

Date: 2026-07-22

## Decision

`RATIFY-PJ-B-PRIME`

The operator ratifies the exact PJ Tranche B-prime candidate as a bounded
exact-native-receipt anti-minting layer over the ratified PJ-A substrate. This
decision ratifies exact revisions, not a branch name. It does not restore the
rejected generic frontier, open a public surface, or choose a final Planet,
Archipelago, Atlas, or Mirage classification.

## Exact candidate

- candidate commit:
  `4e48186cc87249dd5e308e1381345d9758454687`;
- candidate tree:
  `8a7c6bf30da059dfe22e35750ae114fbb35eeccd`;
- candidate parent:
  `a66918dda9dd32892d6275abaa59229633ab3a07`;
- exact changed paths: 16;
- changed-path manifest SHA-256:
  `89294b920331f61a96036ae364bcb50f6338feb82af7b168ac9a5850346a9dc3`;
- declaration-manifest SHA-256:
  `c7544b561271ca64f0c15d8f7c9a980b7cf7eb3da8ce020f0aece3c3aebfebc4`;
- frozen PJ-A Core SHA-256:
  `1d86bee97f92f2644d8605a05d6a31deaa3584f55582d4a09b638718ffff185c`.

The candidate contains 514 compiled owned declarations: 494 axiom-free and
20 exactly `[propext]`, with zero `Quot.sound`, `Classical.choice`, other, or
mixed footprints. The three B-prime modules contain 222 declarations: 216
axiom-free and six generated structure `mk.injEq` declarations exactly
`[propext]`. All 32 named central receipts are axiom-free.

## Ratified scientific boundary

The ratified theorem family distinguishes independently available source
evidence, independently true target evidence, raw bridge inhabitation, exact
native receipt, source-relative entitlement, and receipt-gated consumption.
A bridge-wide `ReceiptFreeMintAt` is constructively refuted by an inhabited
exact native gap. Collapsing the dependent receipt family to `Unit` admits
the challenged lift, and erasing force-bearing receipt identity admits no
constructive left inverse.

The hostile review found and repaired one load-bearing defect before
serialization: the first consumer interface exposed a bare-target observer.
The candidate's final `AdmissibleConsumer` exposes only a force-bearing
operation accepting exact `EntitledFrom`; every positive specialization
derives its observation from native `targetEvidence`.

Ratified specializations:

- Governed Transport: exact route-gap anti-minting, without a runtime
  conformance claim;
- Execution Custody: exact complete-stage anti-minting, with local outcome
  and one-use distinctions retained;
- Someone Continuity: exact reachable-fragment and `AgentId` anti-minting,
  without operational Continuity correspondence;
- held-out StaticRole: faithful partial wrong-wiring specialization, with
  functional dependence remaining local.

## Verification accepted

The operator accepts the serialized receipts showing:

- exact candidate commit, tree, parent, and 16-path surface: pass;
- isolated PJ build: 43 jobs, pass;
- full default aggregate build: 269 jobs, pass;
- direct qualification leaf: 32/32 named receipts axiom-free;
- declaration-manifest regeneration and source-pin checker: pass;
- formalization audit: 19 checks, pass;
- whitespace and path-hash checks: pass;
- frozen PJ-A Core and all source pins: exact.

## Binding limits and forward boundary

This ratification does not establish runtime enforcement, unforgeability,
universal adapter lawfulness, a closed-helper ban on fully covered bridges,
ownership, stateful realization, bridge composition, a residual algebra, or
generic frontier preservation. `FRONTIER-NOT-COMPOSITIONAL` remains
authoritative, and no frontier synonym is admitted.

Tranche C-prime may now examine stateful ownership and context transport. It
must preserve domain-specific residual theories rather than unifying them by
name. No push, tag, mint, publication, release, runtime integration, JCP
implementation, public theory naming, or future successor campaign is
authorized by this record.
