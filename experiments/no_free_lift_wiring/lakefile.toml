name = "wired"
version = "0.1.0"
defaultTargets = ["Wired"]

# Mathlib-free on purpose: the wired composition imports nothing external, so it
# builds with no download and its axiom footprint is fully visible. Pinned to
# v4.29.0 to match canonical ~/git/lean.

[[lean_lib]]
name = "Wired"

# 1.3 candidate: the witnessed-derivation calculus core + model-admission discipline.
# Custody: EXPERIMENTAL-WIRING. NOT in defaultTargets; build `lake build Successor`.
[[lean_lib]]
name = "Successor"
